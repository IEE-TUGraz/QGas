name: QGas
channels:
  - conda-forge
  - defaults
dependencies:
  # Core Python
  - python=3.11.5

  # Image processing for GUI
  - pillow>=10.0.0

  # GUI Framework (built into Python)
  # tkinter is included with Python

  # System utilities
  - pip>=23.0

  # pip packages for specialized dependencies
  - pip:
